{% load warehouse_filters %}
{
"sEcho" : {{sEcho}},
"iTotalRecords" : {{iTotalRecords}},
"iTotalDisplayRecords" : {{iTotalDisplayRecords}},	
"aaData": [
				{% for good in objects %}
				[
				"{{ good.pk }}",
                "{{good.name}}",
				"{{good.submitDate}}",
				"{{ good.category.name }} <input id=\"cat_pk\" type=\"hidden\" value=\"{{good.category.pk}}\" />",
				"<span class=\"{{good.status|good_status_span}}\">{{good.status|good_status}}</span>",
				"{{good.user}}"
				]
				{% if not forloop.last %}
				,
				{% endif %}
                {% endfor %}

			] 



}