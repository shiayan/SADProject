{% load order_filters %}
{
"sEcho" : {{sEcho}},
"iTotalRecords" : {{iTotalRecords}},
"iTotalDisplayRecords" : {{iTotalDisplayRecords}},	
"aaData": [
				{% for order in objects %}
				[
				"{{ order.pk }}",
                "ali",
				"{{ order.submitDate }}",
				"<button  order=\"{{order.pk}}\" role=\"order_modal\"   class=\"btn mini green-stripe\">نمایش</button>"
				]
				{% if not forloop.last %}
				,
				{% endif %}
                {% endfor %}

			] 



}